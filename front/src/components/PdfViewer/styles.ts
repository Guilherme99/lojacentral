import { stitches } from "../../styles/stitches.config";

export const PdfContainer = stitches.styled("div", {
  height: "60vh",
  display: "flex",
  justifyContent: "space-between",
  position: "relative",
});
